# 02_split_and_eda.R: 데이터 분할 및 기초 탐색적 데이터 분석

## 1. 스크립트 개요

`02_split_and_eda.R` 스크립트는 `01_data_preprocessing.R`에서 생성된 통합 전처리 데이터를 입력받아 모델링을 위한 데이터셋을 구축하고, 기초적인 탐색적 데이터 분석(EDA)을 수행합니다.

## 2. 주요 기능

### 2.1. 데이터 분할 (Data Splitting)

-   **입력 데이터**: `data/preprocessed/total_preprocessed.csv`
-   **작업 내용**:
    1.  전체 데이터를 불러온 후, 진단 시 연령을 기준으로 **조기 발병(EOCRC, 50세 미만)** 및 **후기 발병(LOCRC, 50세 이상)** 그룹으로 나눕니다.
    2.  `total`, `EOCRC`, `LOCRC` 세 가지 데이터셋 각각에 대해 `사망여부`를 기준으로 층화추출(stratified sampling)을 적용하여 **훈련 데이터(80%)**와 **검증 데이터(20%)**로 분할합니다.
-   **출력 데이터**:
    -   `data/modeling_datasets/total/`
    -   `data/modeling_datasets/eocrc/`
    -   `data/modeling_datasets/locrc/`
    -   각 폴더 내에 `_train.csv`와 `_valid.csv` 파일 생성

### 2.2. 기초 탐색적 데이터 분석 (Basic EDA)

-   **대상 데이터**: `data/modeling_datasets/total/total_train.csv` (전체 훈련 데이터)
-   **분석 항목**:
    1.  **결측치 분석**: 데이터 내 변수별 결측치 비율을 시각화하여 확인합니다.
    2.  **이상치 분석**: 주요 수치형 변수(연령, CEA 수치, 생존기간 등)에 대한 박스플롯(Boxplot)을 생성하여 이상치 분포를 확인합니다.
    3.  **요약 통계**: 데이터의 기술 통계량(평균, 중앙값, 표준편차 등)을 계산하여 저장합니다.
-   **출력물**:
    -   결측치 및 이상치 분석 그래프 (`results/eda/` 폴더)
    -   요약 통계 텍스트 파일 (`results/eda/00_eda_summary.txt`)

## 3. 실행 흐름

1.  필요한 패키지(`pacman`, `tidyverse`, `caret` 등)를 로드합니다.
2.  `split_and_save` 함수를 정의하여 데이터 분할 및 저장을 자동화합니다.
3.  전체, EOCRC, LOCRC 데이터셋에 대해 `split_and_save` 함수를 실행합니다.
4.  `perform_basic_eda` 함수를 정의하여 기초 EDA 과정을 자동화합니다.
5.  전체 훈련 데이터를 대상으로 `perform_basic_eda` 함수를 실행하여 분석 결과를 저장합니다.


# EDA(탐색적 데이터 분석) 주요 결과 요약

## 1. 수치형 변수 분포

| 변수명             | 최소값 | 1Q     | 중앙값 | 평균   | 3Q     | 최대값   | 결측치(NA) |
|--------------------|--------|--------|--------|--------|--------|----------|------------|
| 진단시 연령        | 12     | 55     | 62     | 62.14  | 70     | 99       | 0          |
| 전체생존기간(일)   | 0      | 381    | 814    | 889.1  | 1331   | 11105    | 328        |
| 진단시점_CEA       | 0.00   | 0.00   | 0.52   | 44.93  | 3.40   | 42737.99 | 23080      |
| 최고_CEA           | 0.00   | 0.26   | 2.82   | 96.92  | 21.69  | 65025.52 | 2191       |

- **진단시 연령:** 전체 분포가 12세부터 99세까지 존재하며, 1사분위수 55세, 중앙값 62세, 3사분위수 70세로 고연령군이 많음.
- **전체생존기간:** 중앙값 814일, 평균 약 2.4년(889일), 최대 30년 이상까지 관찰됨.
- **진단시점_CEA, 최고_CEA:** 중앙값은 낮으나(0.52, 2.82), 평균 및 최대값이 매우 크고, 극단적 이상치 존재. CEA는 결측치(NA) 항목이 상당수 포함.

## 2. 범주형 변수 분포

### (1) 성별

| F(여)   | M(남)   |
|---------|---------|
| 55,949  | 73,180  |

- 남성이 56.7%, 여성이 43.3%

### (2) 최초 진단명

| 진단명(대표적)                        | 빈도  |
|--------------------------------------|-------|
| colon                               | 52,201|
| Malignant neoplasm of colon         | 24,509|
| Malignant neoplasm of rectum        | 14,513|
| rectum                              | 26,471|
| rectosigmoid junction               | 8,256 |
| Malignant neoplasm of rectosigmoid  | 3,179 |

> 진단명이 유사하게 여러 코드·텍스트로 표기되어 있어, 후속 분석 전 표준화 필요

### (3) 병기(Stage)

| 0      | 1      | 2      | 3      | 4      | x      | 결측(NA) |
|--------|--------|--------|--------|--------|--------|-----------|
| 1,674  | 20,303 | 22,367 | 18,406 | 9,638  | 1,736  | 55,005    |

- 병기 정보 미입력(NA) 환자가 43%로 매우 많음(별도 결측 관리 필요)
- Stage 2, 3, 1 순서대로 많음. Stage x(불명확) 포함.

### (4) 치료 변수

#### 수술여부

| 수술 X | 수술 O |
|--------|--------|
| 50,660 | 78,469 |

- 전체의 61%가 수술 경험

#### 항암치료여부

| X      | O      |
|--------|--------|
| 102,707| 26,422 |

- 약 20%만 항암치료 경험(대장암 특성상 충분히 일치)

#### 방사선치료여부

| X      | O      |
|--------|--------|
| 121,676| 7,453  |

- 방사선치료 시행 적음(전체 5.8% 수준)

### (5) 사망여부

| 생존(N) | 사망(Y) |
|---------|---------|
| 114,464 | 14,665  |

- 분석 시점 기준 전체의 약 11.4%에서 사망 이벤트 발생

## 3. 인사이트 및 실무 해석

- **연령 및 생존기간**: 분석코호트가 60~70대 위주(표준 임상연구의 일반적 분포)이며, 생존기간 중앙값은 약 2년, 극소수에서 30년 이상 장기 생존 기록됨.
- **CEA 분포**: 극단치와 결측치가 많으니, 로그 변환·이상치 관리·결측 대체방법 필요.
- **병기/치료 변수**: 병기 결측 비율이 매우 높으므로, 병기별 분석 혹은 다중대치법 전 결측 관리 필수. 치료 변수(수술, 항암, 방사선) 정보는 결측 적음.
- **진단명 변수**: 진단 코드·텍스트의 표준화 및 매핑이 필요(코드북 활용).
- **전체 데이터 품질**: 주요 변수에 결측치가 집중된 부분이 분명히 존재하므로, 결측/이상치/표준화 관리가 핵심.
